var words = ["monkey", "pyramid", "window", "girrafe", "ocean", "friend"];

var word = words[Math.floor(Math.random() * words.length)];

var answerArray = [];
for (var i = 0; i < array.length; i++) {
  const element = array[i];
}
